<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sherafatpour.entekhab.notify">
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="sherafatpour.entekhab.notify.util.GlobalClass"
        android:theme="@style/AppTheme">

        <meta-data android:name="co.ronash.pushe.token" android:value="PUSHE_603569920555"/>
        <service
            android:name="sherafatpour.entekhab.notify.MyPushListener"
            android:exported="false">
            <intent-filter>
                <action android:name="co.ronash.pushe.RECEIVE" />
            </intent-filter>
        </service>
        <activity android:name="sherafatpour.entekhab.notify.AddEditeNoteActivity" android:parentActivityName="sherafatpour.entekhab.notify.MainActivity"></activity>
        <activity android:name="sherafatpour.entekhab.notify.MainActivity" android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>